import { render, screen } from '@testing-libra..act';
import App from ..p';

test('renders learn react link', () => {
  render(<Ap..;
  const linkElement = screen.getByTex..arn rea..;
  expect(linkElement).toBeInTheDocument();
});
